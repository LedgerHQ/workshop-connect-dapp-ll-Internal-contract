{
  "name": "hardhat-project",
  "scripts": {
    "test": "npx hardhat test",
    "test:gas": "REPORT_GAS=1 npm run test",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run --network localhost scripts/deploy.ts",
    "deploy:goerli": "VERIFY=1 npx hardhat run --network goerli scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.1"
  },
  "dependencies": {
    "dotenv": "^16.0.1"
  }
}
